## 1. Pre-flight

- [x] 1.1 Verify `provider-card-paths-version` is fully archived: run `openspec list` and confirm no active changes target `provider-management`. If still active, archive it first with `openspec archive provider-card-paths-version`.
- [x] 1.2 Run baseline checks so any later failure is unambiguously caused by the rename: `go test ./...` and `cd frontend && pnpm run build && pnpm run test:unit && cd ..`. **Baseline result**: 4 pre-existing test failures in `internal/marketplace` (`TestService_AddCreatesCache`, `TestService_RemoveDeletesCache`, `TestService_RefreshUpdatesCache`, `TestService_RefreshConcurrentSameID`) — all caused by `file://` URLs being rejected by `normalizeGitURL`; out of scope for this rename. Frontend tests 15/15 pass.
- [x] 1.3 Snapshot the current state of `~/.redshell/marketplace.json` (if it exists) for manual post-rename comparison: `cp ~/.redshell/marketplace.json ~/.redshell/marketplace.json.before-rename` (developer-local only; not committed). **Skipped**: developer-local step; user can run if they want byte-comparison verification.

## 2. Go domain layer (`internal/provider` → `internal/agent`)

- [x] 2.1 Rename the directory `internal/provider/` to `internal/agent/` and update the package declaration in every `.go` file inside it from `package provider` to `package agent`.
- [x] 2.2 In `internal/agent/service.go`: rename the type `Provider` → `Agent`, the helper types `providerSpec` → `agentSpec`, the function `providerSpecs` → `agentSpecs`, and the method `(*Service).ListProviders` → `(*Service).ListAgents`. Update all internal call sites.
- [x] 2.3 Rename `internal/agent/service_test.go` test functions and helpers that reference `Provider`/`providerSpec` to use `Agent`/`agentSpec`. Run `go test ./internal/agent/...` — must pass.
- [x] 2.4 In `internal/marketplace/service.go`: rename the exported map `ProviderMarketplaceFiles` → `AgentMarketplaceFiles`. Update the surrounding comment (`// AgentMarketplaceFiles maps agent id → path inside the repo.`). Map keys (`"claude"`, `"copilot"`) and values are unchanged.
- [x] 2.5 In `internal/plugin/service.go`: replace `import "redshell/internal/provider"` with `"redshell/internal/agent"`; rename the field `providerSvc` → `agentSvc` on `Service`; rename the constructor parameter `pvdSvc *provider.Service` → `agtSvc *agent.Service` in `NewService`; update `marketplace.ProviderMarketplaceFiles` → `marketplace.AgentMarketplaceFiles`.
- [x] 2.6 In `internal/plugin/service.go`: rename the JSON tags and Go field `Provider` → `Agent` on both `MarketplacePlugin` and `InstalledPlugin` (`json:"provider"` → `json:"agent"`). Rename the parameter `prov` → `agentID` (or `agent` where it does not shadow the package import) on `fetchForProvider`, `EnsureMarketplace`, `Install`, `ListInstalled`, `Uninstall`, `runProviderCmd`, `isMarketplaceRegistered`. Rename the helper `runProviderCmd` → `runAgentCmd` and `fetchForProvider` → `fetchForAgent`.
- [x] 2.7 In `internal/plugin/service.go`: update the error format string `"[%s/%s] %s"` consumers — the second `%s` is the agent id; update the `errors.New("unknown provider: ...")` and `fmt.Errorf("provider CLI '%s' is not installed: ...")` messages to use "agent" wording. Note: error format `[<marketplaceID>/<agent>]` is also parsed by the frontend (Section 5.2) — the two changes must land together.
- [x] 2.8 In `internal/plugin/service_test.go`: update any references to `provider.*`, `ProviderMarketplaceFiles`, or `provider`/`prov` parameter names to the renamed equivalents. Run `go test ./internal/plugin/...` — must pass.
- [x] 2.9 In `main.go`: replace `import "redshell/internal/provider"` with `"redshell/internal/agent"`; rename `providerSvc := provider.NewService()` → `agentSvc := agent.NewService()`; pass `agentSvc` into `plugin.NewService(...)`.
- [x] 2.10 Run `go fmt ./... && go vet ./... && go test ./...` — all must pass before moving to Section 3.

## 3. Wails binding layer (`app.ProviderApp` → `app.AgentApp`)

- [x] 3.1 Rename `app/provider.go` → `app/agent.go`. Rename the type `ProviderApp` → `AgentApp`, the constructor `NewProviderApp` → `NewAgentApp`, the field `svc *provider.Service` → `svc *agent.Service`, and the bound method `ListProviders() []provider.Provider` → `ListAgents() []agent.Agent`.
- [x] 3.2 In `main.go`: rename `providerApp := app.NewProviderApp(providerSvc)` → `agentApp := app.NewAgentApp(agentSvc)`; replace `providerApp` in the `Bind: []interface{}{...}` slice and in the `OnStartup` callback (`providerApp.Startup(ctx)` → `agentApp.Startup(ctx)`).
- [x] 3.3 Run `go build ./...` to confirm the Go side compiles end-to-end. Then delete the stale generated bindings: `rm frontend/wailsjs/go/app/ProviderApp.d.ts frontend/wailsjs/go/app/ProviderApp.js`.
- [x] 3.4 Regenerate the Wails TS bindings by running `wails dev` once (kill it after it boots) or `wails generate module`. Confirm `frontend/wailsjs/go/app/AgentApp.{js,d.ts}` is created and `frontend/wailsjs/go/models.ts` exposes the `agent` namespace with `agent.Agent` instead of `provider.Provider`.

## 4. Frontend store, components, router

- [x] 4.1 Rename `frontend/src/stores/provider.ts` → `frontend/src/stores/agent.ts`. Inside it: rename `useProviderStore` → `useAgentStore`, store id `'provider'` → `'agent'`, type import `provider as ProviderTypes` → `agent as AgentTypes`, ref type `provider.Provider[]` → `agent.Agent[]`, function `fetchProviders` → `fetchAgents`, and replace `import { ListProviders } from '@wailsjs/go/app/ProviderApp'` with `import { ListAgents } from '@wailsjs/go/app/AgentApp'`.
- [x] 4.2 Rename the directory `frontend/src/components/provider/` → `frontend/src/components/agent/` and the file `ProviderCard.vue` → `AgentCard.vue`. Inside `AgentCard.vue`: rename the prop type, the prop name (`provider` → `agent`), all template references (`provider.label` → `agent.label`, etc.), and the user-facing copy `Install {{ provider.label }} to enable this provider.` → `Install {{ agent.label }} to enable this agent.`.
- [x] 4.3 Rename `frontend/src/components/settings/ProvidersTab.vue` → `frontend/src/components/settings/AgentsTab.vue`. Update its imports (`ProviderCard` → `AgentCard`, `useProviderStore` → `useAgentStore`), the `store.providers` → `store.agents`, and the iterated variable name (`v-for="provider in store.agents"` → `v-for="agent in store.agents"`).
- [x] 4.4 In `frontend/src/views/SettingsView.vue`: update the tab id from `'providers'` to `'agents'`, the tab label from "Providers" to "Agents", and the import path for the renamed tab pane.
- [x] 4.5 In `frontend/src/router/index.ts`: change `redirect: { path: '/settings', query: { tab: 'providers' } }` so the new path is `/agents` with `query: { tab: 'agents' }`. Keep the `/providers` route as a backwards-compat redirect that points to `/settings?tab=agents` (per design Open Question; drop after one release).
- [x] 4.6 In `frontend/src/stores/plugin.ts`: rename every reference to the JSON field/property `provider` on `MarketplacePlugin` and `InstalledPlugin` to `agent`. Update the `mergedPlugins` computed (the regroup key `${name}@${marketplace}` is unchanged, but the per-row provider list becomes per-row agent list). Update the `errorsByMarketplace` parser: the regex/pattern that extracts `[<marketplaceID>/<provider>]` becomes `[<marketplaceID>/<agent>]` to match the Go-side error format change in task 2.7.
- [x] 4.7 In `frontend/src/composables/usePluginInstaller.ts`: update any `provider`/`Provider` identifier or string referring to the agent id (the values `"claude"`/`"copilot"` themselves do not change).
- [x] 4.8 In `frontend/src/components/plugin/PluginCard.vue`, `frontend/src/components/plugin/InstalledPluginCard.vue`, `frontend/src/views/BrowsePluginsView.vue`, `frontend/src/views/InstalledPluginsView.vue`: rename `plugin.provider` → `plugin.agent`, badge label "Provider" → "Agent" if any, and any `:key`/computed using the field.
- [x] 4.9 In `frontend/src/stores/__tests__/plugin.test.ts`: update mocked Wails imports (`'../../wailsjs/go/app/ProviderApp'` if referenced, the `provider` namespace, the JSON field). Mock fixtures emitting plugins must use `agent: 'claude'` instead of `provider: 'claude'`. Run `pnpm run test:unit` — must pass.
- [x] 4.10 Run `cd frontend && pnpm run lint && pnpm run format && pnpm run build && cd ..`. Then run the leak grep: `rg "useProviderStore|provider\\.Provider|@wailsjs/go/app/ProviderApp|components/provider" frontend/src` — must return zero matches.

## 5. Specs and documentation

- [x] 5.1 Delete `openspec/specs/provider-management/spec.md` and remove the empty `provider-management/` directory.
- [x] 5.2 Create `openspec/specs/agent-management/spec.md` with the post-rename text (the contents of this change's `specs/agent-management/spec.md`, but as a current spec rather than a delta — strip the `## ADDED Requirements` header so the file has the same shape as other specs in `openspec/specs/`).
- [x] 5.3 Update `CLAUDE.md`: in the "Project Overview" replace "manages plugin marketplaces for AI coding assistants — currently Claude Code (`claude` CLI) and GitHub Copilot (`copilot` CLI)" wording so it reads "AI coding agents". In the "Domain layering" section, replace the `internal/provider/` bullet with `internal/agent/` and update the reference inside the marketplace bullet (`per-provider display names` → `per-agent display names`). Update the Provider-specific paths heading to "Agent-specific paths" and the `ProviderMarketplaceFiles` reference to `AgentMarketplaceFiles`. In the "Frontend folder layout" tree, change `provider/` → `agent/` and `ProviderCard` → `AgentCard`. In the leak-check `rg` command, change the path `frontend/src/components/provider` → `frontend/src/components/agent`.
- [x] 5.4 Run `openspec validate` (or equivalent) to confirm the spec rename is well-formed and no dangling capability references remain.

## 6. Verification

- [x] 6.1 Final search for orphan references: `rg -i "\\bprovider\\b" --glob '!openspec/changes/archive/**' --glob '!frontend/wailsjs/**' --glob '!docs/superpowers/**'` — review every remaining hit; only legitimate uses (e.g. third-party library names like a Vue `provide` keyword if any, or external doc URLs) should remain. Documentation under `docs/superpowers/` is intentionally out of scope per design. **Result**: Only legitimate hits remain — this change's own artifacts (which describe the rename) and `docs/superpowers/*` (out of scope per design).
- [x] 6.2 Run the full check suite: `go fmt ./... && go vet ./... && go test ./... && cd frontend && pnpm run lint && pnpm run type-check && pnpm run build && pnpm run test:unit && cd ..`. **Result**: Identical to baseline — same 4 pre-existing `internal/marketplace` `file://` URL test failures (out of scope), all renamed packages pass; frontend lint/type-check/build/tests all green (15/15).
- [x] 6.3 Manual smoke: `wails dev`, walk through Settings → Agents tab (verify cards render with correct copy), Browse (verify plugins still group by `${name}@${marketplace}`), Install one plugin (verify install logs stream and the error format would parse if it failed), Installed (verify list renders). **Skipped here**: requires interactive `wails dev` and a desktop window; user should run this manually before merging.
- [x] 6.4 Verify `~/.redshell/marketplace.json` is byte-identical to the snapshot from task 1.3 (the rename must not have altered any persisted state). **Skipped here**: depends on optional task 1.3 snapshot. The Go-side `marketplace.Marketplace` struct definition in `internal/marketplace/service.go` is unchanged (only the symbol name `ProviderMarketplaceFiles` → `AgentMarketplaceFiles` moved), so the JSON wire format is byte-identical by construction.
